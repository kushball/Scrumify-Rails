//cache objects we may reuse
var task_details = $("#task_details");
var story_details = $("#story_details");
var task_content = task_details.find("#content");
var stories = $("#stories");

//fade out current content and replace it with new content
task_content.html("<%= escape_javascript(render(:partial => 'tasks/show', :locals => {:task => @task} )) %>");

task_content.fadeOut(300, function() {
  $(this).html("<%= escape_javascript(render(:partial => 'tasks/show', :locals => {:task => @task} )) %>");
});

//stories.css("width", "20%");
//story_details.css("width", "35%");
//task_details.css("width", "35%");

//fade in content after replacement has completed
task_content.fadeIn(200);


